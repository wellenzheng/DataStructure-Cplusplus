#include<algorithm>
#include<vector>
#include<math.h>

using namespace std;

class Utils
{
    private:
        /* data */
    public:
        Utils(/* args */);
        ~Utils();
        int getFeatureMid(int nums[], int low, int high);
        double calculateIGain(vector<vector<int>> dataset, int featureId);

};

Utils::Utils(/* args */)
{
}

Utils::~Utils()
{
}

 
double Utils::calculateIGain(vector<vector<int>> dataset, int featureId)
{
    
}
